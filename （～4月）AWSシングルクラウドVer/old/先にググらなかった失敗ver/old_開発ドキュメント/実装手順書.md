# 実装手順書（AWSシングルクラウド版）

本書は、AWSシングルクラウド構成でNorthwindデータレイクハウスを構築する際の実施順序をまとめたものです。

---

## Phase 0: Databricks アカウントの作成と準備（初回のみ）

AWSの設定を始める前に、Databricks公式サイトからアカウントを作成し、アカウントIDを取得します。

1. [Databricks 無料トライアルページ](https://databricks.com/jp/try-databricks) にアクセスし、クラウドプロバイダーとして **AWS** を選択してアカウントを作成します。
2. アカウント作成後に自動ログイン、もしくは [Databricks Account Console](https://accounts.cloud.databricks.com/) にログインします。
3. 画面右上のユーザー名（またはアイコン）をクリックし、メニュー内にある **Account ID**（英数字の文字列）をコピーして手元に控えます。（Phase 1で使用します）

---

## Phase 1: AWS インフラ構築（1回のみ）

### Step 1-1: CloudFormationスタック作成

```bash
aws cloudformation create-stack \
  --stack-name northwind-lakehouse \
  --template-body file://開発ドキュメント/cloudformation.yaml \
  --parameters \
    ParameterKey=DBUsername,ParameterValue=dbadmin \
    ParameterKey=DBPassword,ParameterValue=<your-password> \
    ParameterKey=DatabricksAccountId,ParameterValue=<your-databricks-account-id> \
  --capabilities CAPABILITY_NAMED_IAM
```

**作成されるリソース**: VPC, Subnet (Public+Private x2), NAT Gateway, RDS PostgreSQL (Private), S3 Bucket, IAM Role, Secrets Manager

**確認方法**:
```bash
aws cloudformation describe-stacks --stack-name northwind-lakehouse --query "Stacks[0].StackStatus"
# → "CREATE_COMPLETE" になるまで待機
```

**Output取得**:
```bash
aws cloudformation describe-stacks --stack-name northwind-lakehouse --query "Stacks[0].Outputs"
# 以降のステップで使用する値を控える:
# - RDSEndpoint
# - S3BucketName
# - DatabricksUnityRoleArn
```

→ 詳細: `開発ドキュメント/環境構築手順書.md`

---

## Phase 2: Databricks ワークスペース設定（1回のみ）

### Step 2-1: Databricksワークスペース作成

1. **事前準備（権限設定）**: 同階層の **`databricks_workspace_policy.json`** を用いてAWS上でクロスアカウント用のIAMロールを作成し、Databricks側の「Credentials」として登録します。
2. **ワークスペース構築**: AWS Databricks コンソールからワークスペースを作成。Phase 1 で作成した VPC, Private Subnet, Security Group を指定します。

### Step 2-2: クラスター作成

| 項目 | 設定 |
|------|------|
| アクセスモード | **専用（Single User）** |
| ノードタイプ | 最小構成（学習用） |
| Unity Catalog | 有効 |

### Step 2-3: Storage Credential 作成（UIから）

1. Databricks UI → **Data** → **Storage Credentials**
2. **Create credential** → IAM Role を選択
3. IAM Role ARN: Phase 1 の Output `DatabricksUnityRoleArn` を入力
4. 名前: `aws_s3_credential`

> ⚠️ **SQLではなくUIから作成してください**。SQLでは環境依存エラーが発生する場合があります。

### Step 2-4: カタログ作成（UIまたはSQL）

```sql
CREATE CATALOG IF NOT EXISTS northwind_catalog;
```

### Step 2-5: Secrets 設定

Databricks CLI で Secret Scope を作成し、RDS接続情報を登録:

| Key | 値の取得元 |
|-----|----------|
| `rds-host` | CloudFormation Output: `RDSEndpoint` |
| `rds-user` | `dbadmin` |
| `rds-password` | 設定したパスワード |

→ 詳細: `開発ドキュメント/環境構築手順書.md` §6

---

## Phase 3: Unity Catalog 初期設定（1回のみ）

### Step 3-1: Notebook `00_setup_unity_catalog.py` を実行

**実行前の確認**:
- [ ] Step 2-3 の Storage Credential が存在する
- [ ] Step 2-4 のカタログが存在する

**処理内容**:
- External Location 作成 (ext_bronze / ext_silver / ext_gold / ext_ops)
- Schema 作成 (bronze / silver / gold / ops)

**完了確認**:
```sql
SHOW EXTERNAL LOCATIONS;
SHOW SCHEMAS IN northwind_catalog;
```

→ ファイル: `開発ドキュメント/notebooks/00_setup_unity_catalog.py`

---

## Phase 4: Northwindデータ投入（1回のみ）

### Step 4-1: Notebook `01_load_northwind_to_rds.py` を実行

**処理内容**:
- GitHub から `northwind.sql` を自動ダウンロード
- RDSに全14テーブル + 全データを投入

**データソース**: [pthom/northwind_psql](https://github.com/pthom/northwind_psql)

**完了確認（期待件数）**:

| テーブル | 件数 |
|---------|------|
| categories | 8 |
| customers | 91 |
| employees | 9 |
| suppliers | 29 |
| shippers | 3 |
| products | 77 |
| orders | 830 |
| order_details | 2,155 |
| region | 4 |
| territories | 53 |
| us_states | 51 |
| employee_territories | 49 |
| customer_demographics | 0 |
| customer_customer_demo | 0 |

→ ファイル: `開発ドキュメント/notebooks/01_load_northwind_to_rds.py`

---

## Phase 5: ETL パイプライン実行（日次）

### Step 5-1: Bronze 取り込み

Notebook `02_etl_bronze_ingest.py` を実行

- RDS全14テーブルをBronze層に取り込み
- メタデータカラム付与 (_run_id, _load_date, _ingest_ts, _source_system)
- `ops.ingestion_log` にログ記録

### Step 5-2: Silver 変換

Notebook `03_etl_silver_transform.py` を実行

- クレンジング・型変換・正規化
- `line_total` 計算カラム追加
- DQチェック → `ops.dq_results` にログ記録

### Step 5-3: Gold 集計

Notebook `04_etl_gold_aggregate.py` を実行

- 4マートテーブル作成 (sales_by_product / sales_by_customer / sales_by_category / order_summary)
- `ops.job_runs` にジョブ完了ログ記録

---

## 実施順序まとめ

```
Phase 0: Databricks アカウント作成
  └─→ Phase 1: CloudFormation デプロイ
        └─→ Phase 2: Databricks ワークスペース + Storage Credential + Secrets
              └─→ Phase 3: 00_setup_unity_catalog.py
                    └─→ Phase 4: 01_load_northwind_to_rds.py
                          └─→ Phase 5: 02 → 03 → 04 (日次繰り返し)
```

| Phase | 実施タイミング | 所要時間目安 |
|-------|-------------|------------|
| 0 | 初回のみ | 〜5分（アカウント作成） |
| 1 | 初回のみ | 〜15分（CloudFormation待ち） |
| 2 | 初回のみ | 〜30分（UI操作含む） |
| 3 | 初回のみ | 〜5分 |
| 4 | 初回のみ | 〜5分 |
| 5 | 日次 | 〜10分 |

---

## 関連ドキュメント

| ドキュメント | 参照タイミング |
|------------|--------------|
| `設計ドキュメント/要件定義書.md` | 全体の要件を確認するとき |
| `設計ドキュメント/システム構成図.md` | インフラ構成を確認するとき |
| `設計ドキュメント/UnityCatalog設計書.md` | Phase 2-3 で参照 |
| `開発ドキュメント/環境構築手順書.md` | Phase 1-2 の詳細手順 |
| `開発ドキュメント/Notebook仕様書.md` | 各Notebookの入出力仕様 |
| `開発ドキュメント/テスト計画書.md` | テスト実施時 |

---

## 変更履歴

| 日付 | 変更内容 |
|------|----------|
| 2026-03-08 | 初版作成 |
