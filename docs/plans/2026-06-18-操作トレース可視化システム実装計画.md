# 操作トレース可視化システム 実装計画

> **For Claude:** REQUIRED SUB-SKILL: Use devops-skills:executing-plans to implement this plan task-by-task.

**Goal:** Northwind ローカル開発環境（FastAPI＋PostgreSQL）の利用者操作を、内部の通信・関数・SQL・テーブルまで辿れる形でトレースし、リアルタイム（3秒以内）＋時間窓集計で可視化する学習ツールを実装する。

**Architecture:** 軽量自前トレーサー。フロントは `window.fetch` をパッチして全通信に trace_id を付与。バックエンドは ASGIミドルウェア＋`_require_token`＋`db.py` を計装して span を生成し、`trace_data/*.jsonl` に追記。SSE で可視化ビューへ 3秒以内に push し、履歴はロールアップJSONLから集計表示。経路マップは事前定義 JSON。

**Tech Stack:** Python 3.13 / FastAPI / Starlette（ASGIミドルウェア・StreamingResponse による SSE）/ psycopg2 / contextvars / バニラ JS（SVG＋Canvas、外部CDN非依存）/ JSONL。

**参照：** 要件定義書 [../design/app_development/操作トレース可視化システム_要件定義書.md](../design/app_development/操作トレース可視化システム_要件定義書.md) ／ 見た目の原型 [../design/app_development/操作トレース可視化システム_UIモックアップ.html](../design/app_development/操作トレース可視化システム_UIモックアップ.html)

---

## オリエンテーション（前提知識ゼロの実装者向け）

- **起動方法：** リポジトリ直下から `cd src && uvicorn api.main:app --reload --port 8000`。または直下の `start_store.bat` / `start_admin.bat`。
- **DB：** ローカル PostgreSQL（`northwind` / `postgres` / `postgres`）。`load_northwind.bat` でロード済み前提。
- **計装する対象（既存コード）：**
  - 全API・静的配信・CORS・認証：`src/api/main.py`
  - DBアクセス：`src/api/db.py`（`fetch_all` / `fetch_one` / `execute`）
  - フロントの通信：`src/web/js/api.js`（`api._fetch`）と各HTMLのインラインJS
- **テスト方針（重要）：** 本リポジトリには**テストランナーが無い**（CLAUDE.md / 要件 C-2）。本計画では次の2層で検証する：
  1. **純粋関数（テーブル抽出・ロールアップ集計）** → `tests/trace/` に**プレーンな assert スクリプト**を置き `python tests/trace/xxx.py` で実行（pytest不要）。
  2. **結合（ミドルウェア・SSE・UI）** → uvicorn を起動し、ブラウザ／`curl` で実挙動を観察。各タスクに具体的手順を記載。
- **コミット方針：** main 直push禁止。**作業ブランチ `feat/trace-visualizer` を切ってから**着手（Task 0）。タスクごとに小さくコミット。
- **新規データ：** `trace_data/`（JSONL出力先）は **gitignore** する。

### 成果物ファイル一覧

| 種別 | パス | 役割 |
|---|---|---|
| 新規 | `src/api/trace.py` | トレース中核（span生成・contextvar・JSONL追記・テーブル抽出・SSE pub/sub・ロールアップ） |
| 新規 | `src/api/trace_routes.py` | `/trace/stream`(SSE)・`/trace/map`・`/trace/rollup` のルーター |
| 新規 | `src/api/trace_map.json` | 事前定義の経路マップ（ノード/エッジ＋初心者向け解説） |
| 新規 | `src/web/js/trace-client.js` | `window.fetch` パッチ＋クリック起点ID付与 |
| 新規 | `src/web/trace/index.html` | 可視化ビュー（モックアップが原型） |
| 新規 | `src/web/trace/viewer.js` | ビューのロジック（SSE購読＝リアルタイム／rollup取得＝履歴） |
| 変更 | `src/api/main.py` | ミドルウェア追加・ルーター取込・`_require_token` 計装 |
| 変更 | `src/api/db.py` | `fetch_all`/`fetch_one`/`execute` を span 化 |
| 変更 | 各 `src/web/**/**.html`（9枚） | `trace-client.js` を読み込む `<script>` 追加 |
| 新規 | `tests/trace/test_*.py` | 純粋関数の assert スクリプト |
| 変更 | `.gitignore` | `trace_data/` を除外 |

---

## フェーズ構成（依存順）

1. **Phase 0** 準備（ブランチ・gitignore）
2. **Phase 1** トレース中核 `trace.py`（純粋関数からTDD）
3. **Phase 2** バックエンド計装（middleware → auth → db.py）
4. **Phase 3** 配信・API（SSE・map・rollup ルーター）
5. **Phase 4** フロント計装（trace-client.js → 各HTML読込）
6. **Phase 5** 可視化ビュー（リアルタイム → 履歴）
7. **Phase 6** ロールアップ生成と保持期間
8. **Phase 7** 仕上げ（失敗経路の赤表示 FR-U8・解説の作り込み）

---

## Phase 0：準備

### Task 0.1: 作業ブランチと gitignore

**Files:**
- Modify: `.gitignore`

**Step 1: ブランチ作成**

```bash
git checkout -b feat/trace-visualizer
```

**Step 2: gitignore に出力先を追加**

`.gitignore` の末尾に追記：

```gitignore
# 操作トレース可視化システムの出力（生イベント/ロールアップJSONL）
trace_data/
```

**Step 3: コミット**

```bash
git add .gitignore
git commit -m "chore(trace): add trace_data/ to gitignore and start feature branch"
```

---

## Phase 1：トレース中核（`src/api/trace.py`）

純粋関数（テーブル抽出）から TDD で始め、その後に状態を持つ部分（contextvar・collector）を足す。

### Task 1.1: SQL からの対象テーブル抽出（TDD）

**Files:**
- Create: `src/api/trace.py`
- Test: `tests/trace/test_extract_tables.py`

**Step 1: 失敗するテストを書く**

`tests/trace/test_extract_tables.py`:

```python
import sys, pathlib
sys.path.insert(0, str(pathlib.Path(__file__).resolve().parents[2] / "src"))
from api.trace import extract_tables

def check(sql, expected):
    got = extract_tables(sql)
    assert got == expected, f"\nSQL: {sql}\n期待: {expected}\n実際: {got}"

# FROM + 複数 JOIN
check(
    "SELECT p.* FROM products p LEFT JOIN categories c ON p.category_id=c.category_id "
    "LEFT JOIN suppliers s ON p.supplier_id=s.supplier_id",
    ["products", "categories", "suppliers"],
)
# INSERT INTO
check("INSERT INTO orders (order_id) VALUES (%s)", ["orders"])
# UPDATE
check("UPDATE products SET units_in_stock=%s WHERE product_id=%s", ["products"])
# 抽出不能 → 「不明」（要件 FR-T4）
check("SELECT 1", ["不明"])

print("OK: extract_tables")
```

**Step 2: 失敗を確認**

Run: `python tests/trace/test_extract_tables.py`
Expected: FAIL（`ModuleNotFoundError: api.trace` または `ImportError: extract_tables`）

**Step 3: 最小実装**

`src/api/trace.py`（新規・冒頭）:

```python
"""操作トレース可視化システム — トレース中核。

責務:
- SQL からの対象テーブル抽出
- contextvar による「現在のトレース」管理
- span の生成と JSONL 追記
- SSE 購読者への配信（スレッド安全）
- ロールアップ集計
計測の失敗は決して被観測アプリを止めない（要件 FR-T6 / NFR-R1）。
"""

import re

# FROM / JOIN / INTO / UPDATE の直後に来るテーブル名を拾う。
# スキーマ修飾(schema.table)・引用符・別名は落としてテーブル名だけ返す。
_TABLE_RE = re.compile(
    r"\b(?:FROM|JOIN|INTO|UPDATE)\s+([A-Za-z_][\w\.]*)",
    re.IGNORECASE,
)


def extract_tables(sql: str) -> list[str]:
    """SQL文から対象テーブル名を順序維持・重複排除で抽出する。
    1つも取れなければ ['不明'] を返す（要件 FR-T4）。"""
    if not sql:
        return ["不明"]
    found: list[str] = []
    for m in _TABLE_RE.finditer(sql):
        name = m.group(1).split(".")[-1].strip('"')
        if name and name.lower() not in {"select"} and name not in found:
            found.append(name)
    return found or ["不明"]
```

**Step 4: 成功を確認**

Run: `python tests/trace/test_extract_tables.py`
Expected: `OK: extract_tables`

**Step 5: コミット**

```bash
git add src/api/trace.py tests/trace/test_extract_tables.py
git commit -m "feat(trace): SQL からの対象テーブル抽出（失敗時は不明）"
```

---

### Task 1.2: span モデルと contextvar

**Files:**
- Modify: `src/api/trace.py`

**Step 1: 実装を追記**

`src/api/trace.py` に追記:

```python
import contextvars
import itertools
import time
import uuid

# 現在処理中のトレース文脈（middleware が設定、db.py 計装が参照）。
# async→threadpool 間も contextvars はコピーされるため sync エンドポイントからも見える。
_current = contextvars.ContextVar("trace_ctx", default=None)


class TraceCtx:
    """1リクエスト＝1トレースの文脈。span_id を採番する。"""
    def __init__(self, trace_id: str):
        self.trace_id = trace_id
        self._seq = itertools.count(1)
        self.last_span_id = "root"

    def next_span_id(self) -> str:
        return f"s{next(self._seq):03d}"


def new_trace_id() -> str:
    return uuid.uuid4().hex[:8]


def start_trace(trace_id: str | None = None) -> TraceCtx:
    ctx = TraceCtx(trace_id or new_trace_id())
    _current.set(ctx)
    return ctx


def current() -> "TraceCtx | None":
    return _current.get()


def make_span(layer: str, node: str, kind: str, *, detail: str = "",
              tables: list[str] | None = None, dur_ms: float = 0.0,
              status: str = "ok", parent: str = "root") -> dict:
    ctx = current()
    return {
        "trace_id": ctx.trace_id if ctx else new_trace_id(),
        "span_id": ctx.next_span_id() if ctx else "s001",
        "parent_span_id": parent,
        "ts": time.strftime("%Y-%m-%dT%H:%M:%S", time.localtime()) +
              f".{int((time.time() % 1) * 1000):03d}",
        "layer": layer, "node": node, "kind": kind,
        "detail": detail, "tables": tables or [], "dur_ms": round(dur_ms, 2),
        "status": status,
    }
```

**Step 2: 動作確認（簡易スクリプト）**

`tests/trace/test_span.py`:

```python
import sys, pathlib
sys.path.insert(0, str(pathlib.Path(__file__).resolve().parents[2] / "src"))
from api import trace

trace.start_trace("abc12345")
s1 = trace.make_span("db", "h_list", "sql", detail="SELECT ...", tables=["products"], dur_ms=12.4)
assert s1["trace_id"] == "abc12345"
assert s1["span_id"] == "s001"
assert s1["tables"] == ["products"]
s2 = trace.make_span("http", "list_products", "http")
assert s2["span_id"] == "s002", s2
print("OK: span model")
```

Run: `python tests/trace/test_span.py`
Expected: `OK: span model`

**Step 3: コミット**

```bash
git add src/api/trace.py tests/trace/test_span.py
git commit -m "feat(trace): span モデルと contextvar による文脈管理"
```

---

### Task 1.3: JSONL 追記コレクタ＋SSE pub/sub（スレッド安全）

**Files:**
- Modify: `src/api/trace.py`

**Step 1: 実装を追記**

```python
import asyncio
import json
import threading
from pathlib import Path

# 出力先: リポジトリ直下 trace_data/（db.py と同じ parents[2] 基準）
_DATA_DIR = Path(__file__).resolve().parents[2] / "trace_data"
_DATA_DIR.mkdir(exist_ok=True)
_write_lock = threading.Lock()

# SSE 購読者（可視化ビュー）の asyncio.Queue 群と、メインイベントループ参照。
_subscribers: set[asyncio.Queue] = set()
_loop: asyncio.AbstractEventLoop | None = None


def bind_loop(loop: asyncio.AbstractEventLoop) -> None:
    """起動時にメインイベントループを記録（worker thread から push するため）。"""
    global _loop
    _loop = loop


def _events_path() -> Path:
    return _DATA_DIR / f"events-{time.strftime('%Y%m%d')}.jsonl"


def record(span: dict) -> None:
    """span を JSONL に追記し、SSE 購読者へ push する。
    いかなる失敗もここで握りつぶす（被観測アプリを止めない: FR-T6）。"""
    try:
        line = json.dumps(span, ensure_ascii=False)
        with _write_lock:
            with _events_path().open("a", encoding="utf-8") as f:
                f.write(line + "\n")
        _publish(span)
        _rollup_accumulate(span)  # Task 6.1 で定義
    except Exception:
        pass


def _publish(span: dict) -> None:
    if not _loop:
        return
    for q in list(_subscribers):
        try:
            _loop.call_soon_threadsafe(q.put_nowait, span)
        except Exception:
            pass


def subscribe() -> asyncio.Queue:
    q: asyncio.Queue = asyncio.Queue(maxsize=1000)
    _subscribers.add(q)
    return q


def unsubscribe(q: asyncio.Queue) -> None:
    _subscribers.discard(q)


# Task 6.1 までの暫定スタブ（後で本実装に置換）
def _rollup_accumulate(span: dict) -> None:
    pass
```

**Step 2: 動作確認**

`tests/trace/test_record.py`:

```python
import sys, pathlib, json
sys.path.insert(0, str(pathlib.Path(__file__).resolve().parents[2] / "src"))
from api import trace

trace.start_trace("rec00001")
trace.record(trace.make_span("db", "h_list", "sql", tables=["products"]))
p = trace._events_path()
last = p.read_text(encoding="utf-8").strip().splitlines()[-1]
obj = json.loads(last)
assert obj["trace_id"] == "rec00001" and obj["tables"] == ["products"], obj
print("OK: record -> jsonl")
```

Run: `python tests/trace/test_record.py`
Expected: `OK: record -> jsonl`（`trace_data/events-YYYYMMDD.jsonl` が生成される）

**Step 3: コミット**

```bash
git add src/api/trace.py tests/trace/test_record.py
git commit -m "feat(trace): JSONL追記コレクタとSSE pub/sub（スレッド安全・失敗握りつぶし）"
```

---

## Phase 2：バックエンド計装

### Task 2.1: ASGI トレースミドルウェア（全リクエストを span 化）

**Files:**
- Modify: `src/api/main.py`

**Step 1: 実装**

`src/api/main.py` の import 群に追加:

```python
import time
from api import trace
```

`app = FastAPI(...)` の後、CORS 追加の前後どちらでもよいので追記:

```python
@app.on_event("startup")
async def _trace_bind_loop():
    import asyncio
    trace.bind_loop(asyncio.get_running_loop())


@app.middleware("http")
async def trace_middleware(request, call_next):
    # フロントが付けた X-Trace-Id があれば 1クリックとして引き継ぐ。無ければ新規採番。
    trace_id = request.headers.get("X-Trace-Id") or trace.new_trace_id()
    trace.start_trace(trace_id)
    t0 = time.perf_counter()
    status = "ok"
    try:
        response = await call_next(request)
        if response.status_code >= 400:
            status = "error"
        return response
    except Exception:
        status = "error"
        raise
    finally:
        dur = (time.perf_counter() - t0) * 1000
        path = request.url.path
        # 静的配信や /trace 自身は記録しない（ノイズ・自己観測ループ防止）
        if path.startswith("/api"):
            trace.record(trace.make_span(
                "http", path, "http",
                detail=f"{request.method} {path}",
                dur_ms=dur,
                status=status,
            ))
```

**Step 2: 結合確認**

```bash
cd src && uvicorn api.main:app --port 8000   # 別ターミナル
curl -s http://localhost:8000/api/products -H "X-Trace-Id: testtrace" > /dev/null
```

`trace_data/events-YYYYMMDD.jsonl` の末尾に `"trace_id":"testtrace"`, `"kind":"http"`, `"node":"/api/products"` の行があることを確認。

**Step 3: コミット**

```bash
git add src/api/main.py
git commit -m "feat(trace): ASGIミドルウェアで全APIリクエストをspan化"
```

---

### Task 2.2: 認証 `_require_token` の計装（通過/失敗）

**Files:**
- Modify: `src/api/main.py:52`（`_require_token`）

**Step 1: 実装**

`_require_token` を次のように計装（span を足すだけで挙動は不変）:

```python
def _require_token(authorization: str = Header(...)) -> dict:
    """Authorization: Bearer <token> からセッション情報を取り出す。"""
    if not authorization.startswith("Bearer "):
        trace.record(trace.make_span("auth", "_require_token", "auth",
                                     detail="形式不正", status="error"))
        raise HTTPException(status_code=401, detail="認証が必要です")
    token = authorization[7:]
    session = _sessions.get(token)
    if not session:
        trace.record(trace.make_span("auth", "_require_token", "auth",
                                     detail="トークン無効", status="error"))
        raise HTTPException(status_code=401, detail="トークンが無効または期限切れです")
    trace.record(trace.make_span("auth", "_require_token", "auth",
                                 detail="認証OK", status="ok"))
    return session
```

> 注：公開API（`list_products` 等）は `_require_token` を呼ばないため auth span は出ない＝「認証スキップ」を**ノードの不在で表現**する（要件 FR-T3／マップ駆動）。

**Step 2: 結合確認**

```bash
# 認証失敗（トークン無し）
curl -s http://localhost:8000/api/employees -H "Authorization: Bearer xxx" -H "X-Trace-Id: authfail" > /dev/null
```

JSONL に `"node":"_require_token"`, `"status":"error"`, `"detail":"トークン無効"` が出ること。

**Step 3: コミット**

```bash
git add src/api/main.py
git commit -m "feat(trace): 認証 _require_token の通過/失敗をspan化"
```

---

### Task 2.3: `db.py` の計装（SQL・対象テーブル・所要時間）

**Files:**
- Modify: `src/api/db.py`

**Step 1: 実装**

`src/api/db.py` 冒頭の import に追加:

```python
import time
from api import trace
```

3関数を span 記録つきに変更（戻り値・例外挙動は不変。失敗時もトレースが本処理を止めない）:

```python
def fetch_all(query, params=None):
    t0 = time.perf_counter()
    status = "ok"
    try:
        with _cursor() as cur:
            cur.execute(query, params)
            return [dict(r) for r in cur.fetchall()]
    except Exception:
        status = "error"
        raise
    finally:
        _record_sql(query, params, (time.perf_counter() - t0) * 1000, status)


def fetch_one(query, params=None):
    t0 = time.perf_counter()
    status = "ok"
    try:
        with _cursor() as cur:
            cur.execute(query, params)
            row = cur.fetchone()
            return dict(row) if row else None
    except Exception:
        status = "error"
        raise
    finally:
        _record_sql(query, params, (time.perf_counter() - t0) * 1000, status)


def execute(query, params=None):
    t0 = time.perf_counter()
    status = "ok"
    try:
        with _cursor(dict_cursor=False) as cur:
            cur.execute(query, params)
            return cur.rowcount
    except Exception:
        status = "error"
        raise
    finally:
        _record_sql(query, params, (time.perf_counter() - t0) * 1000, status)


def _record_sql(query, params, dur_ms, status):
    """SQL span を記録。トレース失敗は握りつぶす（FR-T6）。"""
    try:
        n = len(params) if params is not None else 0
        trace.record(trace.make_span(
            "db", "db.py", "sql",
            detail=" ".join(str(query).split()),  # 改行を畳む
            tables=trace.extract_tables(str(query)),
            dur_ms=dur_ms,
            status=status,
        ))
    except Exception:
        pass
```

**Step 2: 結合確認**

```bash
curl -s http://localhost:8000/api/products -H "X-Trace-Id: dbtest" > /dev/null
```

同一 `trace_id":"dbtest"` で **http span と sql span の両方**が JSONL に出て、sql span の `tables` に `["products","categories","suppliers"]` が入ること。

**Step 3: コミット**

```bash
git add src/api/db.py
git commit -m "feat(trace): db.py の fetch/execute をSQL span化（対象テーブル抽出つき）"
```

---

## Phase 3：配信・API ルーター

### Task 3.1: 経路マップ定義 `trace_map.json`

**Files:**
- Create: `src/api/trace_map.json`

**Step 1: 作成**

UIモックアップの `N`（ノード）・`DETAIL`（初心者解説）・`ACTIONS`（経路）を JSON 化して移植する。構造:

```json
{
  "columns": ["画面","fetch","CORS","認証","ハンドラ関数","db.py","psycopg2","テーブル"],
  "nodes": {
    "catalog": {"col":0,"label":"catalog.html","sub":"商品一覧画面",
      "detail": {"k":"画面 (HTML)","f":"src/web/store/catalog.html",
        "one":"あなたが今見ている画面そのもの。",
        "easy":"... モックアップの easy 文をそのまま ...",
        "terms":[["HTML","エイチティーエムエル","..."]]}}
    /* ... 以下、モックアップの全ノードを移植 ... */
  },
  "edges": [["catalog","fetch"],["fetch","cors"], "... モックアップ allEdges 相当 ..."]
}
```

> 出典は [操作トレース可視化システム_UIモックアップ.html](../design/app_development/操作トレース可視化システム_UIモックアップ.html) の `N` / `DETAIL` / `ACTIONS`。内容は要件 FR-U7（4段解説）を満たす形でそのまま使う。

**Step 2: JSON 妥当性確認**

Run: `python -c "import json,pathlib; json.loads(pathlib.Path('src/api/trace_map.json').read_text(encoding='utf-8')); print('OK: map json')"`
Expected: `OK: map json`

**Step 3: コミット**

```bash
git add src/api/trace_map.json
git commit -m "feat(trace): 事前定義の経路マップJSON（ノード/エッジ/初心者解説）"
```

---

### Task 3.2: トレースルーター（map / stream(SSE) / rollup）

**Files:**
- Create: `src/api/trace_routes.py`
- Modify: `src/api/main.py`（ルーター取込・ビュー配信は既存 `/static` マウントで自動）

**Step 1: 実装**

`src/api/trace_routes.py`:

```python
"""可視化ビュー向け API: 経路マップ・リアルタイム配信(SSE)・履歴集計。"""

import asyncio
import json
from pathlib import Path

from fastapi import APIRouter, Request
from fastapi.responses import JSONResponse, StreamingResponse

from api import trace

router = APIRouter(prefix="/trace", tags=["trace"])
_MAP = json.loads((Path(__file__).resolve().parent / "trace_map.json").read_text(encoding="utf-8"))


@router.get("/map")
def get_map():
    return JSONResponse(_MAP)


@router.get("/stream")
async def stream(request: Request):
    """Server-Sent Events。新規 span を逐次 push（3秒以内: FR-R1）。"""
    q = trace.subscribe()

    async def gen():
        try:
            while True:
                if await request.is_disconnected():
                    break
                try:
                    span = await asyncio.wait_for(q.get(), timeout=15)
                    yield f"data: {json.dumps(span, ensure_ascii=False)}\n\n"
                except asyncio.TimeoutError:
                    yield ": keep-alive\n\n"
        finally:
            trace.unsubscribe(q)

    return StreamingResponse(gen(), media_type="text/event-stream")


@router.get("/rollup")
def rollup(window: str = "1h"):
    """指定時間窓のエッジ別通過量を返す（進行中バケットも途中経過として含む: FR-S2）。"""
    return JSONResponse(trace.rollup_for(window))  # Task 6.2 で実装
```

`src/api/main.py` にルーター取込（import 群と末尾付近）:

```python
from api.trace_routes import router as trace_router
app.include_router(trace_router)
```

**Step 2: 結合確認**

```bash
curl -s http://localhost:8000/trace/map | python -c "import sys,json;print('nodes',len(json.load(sys.stdin)['nodes']))"
# 別ターミナルで SSE 購読しつつ、さらに別で /api/products を叩くと data: 行が流れる
curl -N http://localhost:8000/trace/stream &
curl -s http://localhost:8000/api/products > /dev/null
```

`/trace/stream` 側に `data: {"trace_id":...,"kind":"http"...}` 等が表示されること。

**Step 3: コミット**

```bash
git add src/api/trace_routes.py src/api/main.py
git commit -m "feat(trace): map/stream(SSE)/rollup ルーターを追加"
```

---

## Phase 4：フロント計装

### Task 4.1: `trace-client.js`（window.fetch パッチ＋クリック起点ID）

**Files:**
- Create: `src/web/js/trace-client.js`

**Step 1: 実装**

```javascript
/* trace-client.js — 全通信に trace_id を付与する計装（被観測アプリ側に読み込む）。
 * 決定#1: クリックを伴わない自動発火通信も含め、全 fetch を対象にする。 */
(function () {
  let currentRoot = null;     // 直近のユーザー操作(クリック)に対応する trace_id
  let rootSetAt = 0;

  function newId() {
    return (Date.now().toString(36) + Math.random().toString(36).slice(2, 6));
  }

  // ユーザー操作（クリック/サブミット）を起点に root を更新。
  // 同一クリックから派生する複数 fetch を 1 trace に束ねる（要件 #6）。
  function markRoot() { currentRoot = newId(); rootSetAt = Date.now(); }
  document.addEventListener('click', markRoot, true);
  document.addEventListener('submit', markRoot, true);

  const _fetch = window.fetch.bind(window);
  window.fetch = function (input, init) {
    init = init || {};
    const headers = new Headers(init.headers || (input instanceof Request ? input.headers : undefined));
    // クリック直後(2秒以内)は同じ root を、そうでなければ自動発火として新規 trace を採番。
    const tid = (currentRoot && (Date.now() - rootSetAt) < 2000) ? currentRoot : newId();
    headers.set('X-Trace-Id', tid);
    return _fetch(input, Object.assign({}, init, { headers }));
  };
})();
```

**Step 2: 単体確認（ブラウザのコンソール）**
`trace-client.js` を読み込んだページで `fetch('/api/products')` を実行し、ネットワークタブのリクエストヘッダに `X-Trace-Id` が付くことを確認（Task 4.2 後に実施でも可）。

**Step 3: コミット**

```bash
git add src/web/js/trace-client.js
git commit -m "feat(trace): フロント window.fetch パッチでX-Trace-Id付与"
```

---

### Task 4.2: 各HTMLに `trace-client.js` を読み込む

**Files（9枚）:**
- Modify: `src/web/store/login.html`, `catalog.html`, `cart.html`, `checkout.html`, `history.html`
- Modify: `src/web/admin/login.html`, `dashboard.html`, `products.html`, `inventory.html`, `employees.html`

**Step 1: 各ファイルの `<head>` 内（他の `<script>` より前）に追加**

```html
<script src="/static/js/trace-client.js"></script>
```

> `window.fetch` を**最優先で**パッチするため、`api.js` 等より前に置く。各ページのパス深さに関わらず絶対パス `/static/js/...` でよい。

**Step 2: 結合確認**
`http://localhost:8000/static/store/catalog.html` を開き、開発者ツールのネットワークで `/api/products` リクエストに `X-Trace-Id` が付き、同一IDで http/sql span が JSONL に出ることを確認。

**Step 3: コミット**

```bash
git add src/web/store/*.html src/web/admin/*.html
git commit -m "feat(trace): 全画面に trace-client.js を読み込み"
```

---

## Phase 5：可視化ビュー

### Task 5.1: ビュー雛形（モックアップを移植）

**Files:**
- Create: `src/web/trace/index.html`
- Create: `src/web/trace/viewer.js`

**Step 1: 移植**
[操作トレース可視化システム_UIモックアップ.html](../design/app_development/操作トレース可視化システム_UIモックアップ.html) を土台に、`index.html`（マークアップ＋CSS）と `viewer.js`（ロジック）へ分割。**この時点ではダミーデータのまま**動けばよい（レイアウト確認）。ただしノード/エッジ/解説は `trace_map.json` から取得するよう、起動時に `fetch('/trace/map')` する。

**Step 2: 確認**
`http://localhost:8000/static/trace/index.html` を開き、マップが `/trace/map` から描画されること（操作ボタンのダミー粒子も従来通り動く）。

**Step 3: コミット**

```bash
git add src/web/trace/index.html src/web/trace/viewer.js
git commit -m "feat(trace): 可視化ビュー雛形（マップは /trace/map から取得）"
```

---

### Task 5.2: リアルタイムモードを SSE に接続

**Files:**
- Modify: `src/web/trace/viewer.js`

**Step 1: 実装**
ダミーの操作ボタン発火を廃し、`EventSource('/trace/stream')` を購読。受信 span を `trace_id` ごとにまとめ、`layer/node` をマップ座標に対応づけて該当エッジを点灯＋粒子を流す。`status==="error"` の span は赤系で表示（FR-U8 の下地）。

```javascript
const es = new EventSource('/trace/stream');
es.onmessage = (e) => {
  const span = JSON.parse(e.data);
  onSpan(span);   // span を受けて該当ノード/エッジを点灯し粒子を流す
};
```

**Step 2: 結合確認**
ビューを開いた状態で、別タブの `catalog.html` で商品一覧を開く → **3秒以内**にビューで該当経路が点灯・粒子が流れること（要件 NFR-P1）。

**Step 3: コミット**

```bash
git add src/web/trace/viewer.js
git commit -m "feat(trace): リアルタイムモードをSSE実データに接続"
```

---

### Task 5.3: 履歴モードを `/trace/rollup` に接続

**Files:**
- Modify: `src/web/trace/viewer.js`

**Step 1: 実装**
時間窓トグル押下で `fetch('/trace/rollup?window=10m|1h|1d|1w|1m|1y')` を取得し、エッジ別 `count` で線の太さ＋ヒート色を更新（モックアップの `paintHistory` を実データ化）。エッジクリックで回数を表示。

**Step 2: 結合確認**
何回か操作したのち履歴モードへ切替→窓を変えると線の太さ/色が変わること。進行中バケットも含まれること（FR-S2）。

**Step 3: コミット**

```bash
git add src/web/trace/viewer.js
git commit -m "feat(trace): 履歴モードを /trace/rollup 実データに接続"
```

---

## Phase 6：ロールアップと保持

### Task 6.1: ロールアップ集計（漸進更新）TDD

**Files:**
- Modify: `src/api/trace.py`（`_rollup_accumulate` 本実装＋`rollup_for`）
- Test: `tests/trace/test_rollup.py`

**Step 1: 失敗テスト**

`tests/trace/test_rollup.py`:

```python
import sys, pathlib
sys.path.insert(0, str(pathlib.Path(__file__).resolve().parents[2] / "src"))
from api import trace

trace._rollup_reset()  # テスト用初期化
# 同一トレースの連鎖（http -> sql）を1回ぶん流す
trace.start_trace("t1")
trace.record(trace.make_span("http", "/api/products", "http"))
trace.record(trace.make_span("db", "db.py", "sql", tables=["products"]))

r = trace.rollup_for("1h")
edges = {e["edge"]: e["count"] for e in r}
# /api/products -> db.py のエッジが1回計上される
assert edges.get("/api/products>db.py", 0) >= 1, r
print("OK: rollup")
```

**Step 2: 失敗確認** → `python tests/trace/test_rollup.py`（`AttributeError: rollup_for` 等）

**Step 3: 実装**
`trace.py` の `_rollup_accumulate` を、trace_id ごとに直前 span との隣接を「エッジ」として時間バケット（分/時/日）別カウンタに加算する形へ。`rollup_for(window)` は対象窓のバケットを合算して `[{"edge","count","avg_ms"}]` を返す。`_rollup_reset()` はテスト用。実データはプロセス内メモリ＋定期フラッシュ（Task 6.3）で `trace_data/rollup-*.jsonl` に保存。

**Step 4: 成功確認** → `python tests/trace/test_rollup.py` → `OK: rollup`

**Step 5: コミット**

```bash
git add src/api/trace.py tests/trace/test_rollup.py
git commit -m "feat(trace): ロールアップ漸進集計と窓別取得"
```

---

### Task 6.2: 起動時の既存イベント取り込み

**Files:**
- Modify: `src/api/main.py`（startup）／`src/api/trace.py`

**Step 1: 実装**
startup で当日の `events-*.jsonl` を読み、`_rollup_accumulate` を再生して起動時点の集計を復元（FR-S2「起動時集計」）。大量行を避けるため当日分のみ対象。

**Step 2: 確認**
イベントがある状態で再起動 → `/trace/rollup?window=1d` が再起動前と同等の件数を返すこと。

**Step 3: コミット**

```bash
git add src/api/main.py src/api/trace.py
git commit -m "feat(trace): 起動時に当日イベントを取り込みロールアップ復元"
```

---

### Task 6.3: 保持期間とフラッシュ

**Files:**
- Modify: `src/api/trace.py`

**Step 1: 実装**
- ロールアップをメモリから `trace_data/rollup-YYYYMMDD.jsonl` へ定期/書込契機でフラッシュ。
- 保持: 生イベント7日・ロールアップ（分1日/時30日/日1年）を超える古いファイルを起動時に削除（NFR-O2）。

**Step 2: 確認**
古い日付のダミー `events-*.jsonl` を置いて起動 → 7日超が削除されること。

**Step 3: コミット**

```bash
git add src/api/trace.py
git commit -m "feat(trace): ロールアップのフラッシュと保持期間管理"
```

---

## Phase 7：仕上げ

### Task 7.1: 失敗経路の赤表示（FR-U8）

**Files:**
- Modify: `src/web/trace/viewer.js`

**Step 1: 実装**
`status==="error"` の span を含むエッジ/ノードをリアルタイム・履歴とも赤系で点灯。凡例にも追加。

**Step 2: 確認**
存在しない認証で保護APIを叩く（401）→ 認証ノードが赤く光ること。

**Step 3: コミット**

```bash
git add src/web/trace/viewer.js
git commit -m "feat(trace): 失敗経路（401/DBエラー）を赤表示（FR-U8）"
```

---

### Task 7.2: 解説パネルの作り込み確認（FR-U7）

**Files:**
- Modify: `src/api/trace_map.json` / `src/web/trace/viewer.js`

**Step 1: 実装/確認**
ノード/エッジ詳細が4段（①一言②やさしい説明③実体・実SQL④用語注釈）で表示され、用語のホバー注釈が機能することを全ノードで確認。モックアップの解説文と齟齬がないか突き合わせ。

**Step 2: コミット**

```bash
git add src/api/trace_map.json src/web/trace/viewer.js
git commit -m "polish(trace): 初心者向け4段解説パネルの整備（FR-U7）"
```

---

### Task 7.3: 受け入れ確認（要件トレース）と README

**Files:**
- Create: `src/web/trace/README.md`（起動と使い方）

**受け入れチェック（要件 → 確認）:**

| 要件 | 確認方法 |
|---|---|
| NFR-P1 3秒以内 | 操作→点灯までの体感が3秒以内 |
| FR-T1 全通信対象 | ページロードの自動通信も JSONL に出る |
| FR-T3 認証 | 公開API=認証ノード無し／保護API成功=緑／失敗=赤 |
| FR-T4 テーブル抽出 | sql span の tables が正しい／不明時「不明」 |
| FR-U2/U3 履歴・切替 | 6窓すべてで太さ/色が変化 |
| FR-U8 失敗赤 | 401で赤点灯 |
| NFR-R1 副作用ゼロ | `trace_data/` を読取専用にしても各アプリが正常動作 |

**コミット:**

```bash
git add src/web/trace/README.md
git commit -m "docs(trace): 可視化ビューのREADMEと受け入れ確認"
```

---

## 完了の定義（Definition of Done）

- 上記全タスクがコミット済み。
- `python tests/trace/*.py` がすべて `OK`。
- ビューを開いた状態で各アプリを操作すると、3秒以内にリアルタイム経路が点灯し、履歴モードで6時間窓の通過量が見られる。
- トレース無効化（`trace_data/` 削除や書込不可）でも被観測アプリが正常動作（FR-T6/NFR-R1）。
- `feat/trace-visualizer` ブランチで PR 作成可能な状態。
